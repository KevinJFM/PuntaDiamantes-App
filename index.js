import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent registra el componente raíz y prepara el entorno para Expo Go y builds nativos.
registerRootComponent(App);
